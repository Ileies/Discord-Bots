{
  "name": "discord-bots",
  "module": "true",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir build --target bun --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "axios": "^1.10.0",
    "discord.js": "^14.21.0",
    "typescript": "^5.8.3"
  }
}