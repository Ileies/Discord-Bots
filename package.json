{
  "name": "discord-bots",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir build --target bun --minify"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "discord.js": "^14.27.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
